apply plugin: "java"

apply plugin: "eclipse"

version = "0.1"

jar {
    manifest {
        attributes 'PCR1000-Backend': 'The PCR1000 Backend',
                   'Implementation-Version': version
    }
}
